# ShootLogix -- Environment Variables
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.

# JWT secret key for token signing. REQUIRED in production.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=

# Database path. Defaults to ./shootlogix.db (local dev).
# On Railway with a volume mounted at /data, set to: /data/shootlogix.db
# DATABASE_PATH=/data/shootlogix.db

# Upload directory for images. Defaults to ./static/uploads (local dev).
# On Railway with a volume mounted at /data, set to: /data/uploads
# UPLOAD_DIR=/data/uploads

# Port (set automatically by Railway, default 5002 for local dev)
# PORT=5002
