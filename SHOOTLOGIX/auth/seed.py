"""
auth/seed.py — Seed default users and project memberships.
Called on every startup; idempotent (only creates if not exists).
"""


def seed_auth_data():
    """Placeholder — actual seeding implemented in Step 2."""
    pass
